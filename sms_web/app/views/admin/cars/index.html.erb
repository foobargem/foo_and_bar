<h2>차량 관리</h2>

<ul class="guide">
  <li>전시되는 차량의 정보를 관리합니다.</li>
  <li>모바일 앱에서 해당 차량의 상세 정보로 보여집니다.</li>
</ul>

<div class="action_container">
  <%= link_to '새로 등록하기', new_admin_car_path %>
</div>

<table class="cars">
  <thead>
    <tr>
      <th class="photo">대표 사진</th>
      <th class="company_name">제조사</th>
      <th class="name">차명</th>
      <th class="voice_stream_url">음성 URL</th>
      <th class="video_stream_url">영상 URL</th>
      <th class="flickr_url">Flickr URL</th>
      <th class="photos">연관 사진</th>
      <th class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <% @cars.each do |car| %>
      <%= render 'admin/cars/tr_car', :car => car %>
    <% end %>
  </tbody>
</table>

<%= will_paginate(@cars) %>
