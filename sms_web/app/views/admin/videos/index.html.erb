<h2>비디오 관리</h2>

<ul class="guide">
  <li>모바일 앱에서 영상보기에서 사용될 동영상 정보를 관리합니다.</li>
</ul>

<div class="action_container">
  <%= link_to 'New Video', new_admin_video_path %>
</div>

<table class="videos">
  <thead>
    <tr>
      <th class="title">제목</th>
      <th class="stream_url">URL</th>
      <th class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <% @videos.each do |video| %>
      <tr>
        <td class="title"><%= video.title %></td>
        <td class="stream_url"><%= video.stream_url %></td>
        <td class="actions">
          <%= link_to 'Edit', edit_admin_video_path(video) %>
          <%= link_to 'Destroy', [:admin, video], :confirm => 'Are you sure?', :method => :delete %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate(@videos) %>
